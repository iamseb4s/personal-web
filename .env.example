# This file serves as a template for the environment variables required by the project.
#
# For local development, copy this file to a new file named .env.development
# and fill in the required values.
#
# For staging, copy this file to a new file named .env.staging
# and fill in the required values.
#
# For production, copy this file to a new file named .env.production
# and fill in the required values.
#
# Do NOT commit .env.* files to version control.

# --- Development Variables ---
# These are used when running the application in the development environment.

# Your Google Cloud Project ID.
GOOGLE_CLOUD_PROJECT=your-project-id

# --- Staging Variables ---
# These are used when running the application in the staging environment.
# (No file-based variables required for staging at this time)

# --- Production Variables ---
# These are used when running the application in the production environment.
# (No file-based variables required for production at this time)
