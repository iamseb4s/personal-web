services:
  prod-web:
    container_name: web-prod
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - "4000:4000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production