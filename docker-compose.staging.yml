services:
  staging-web:
    container_name: web-staging
    build:
      context: .
      dockerfile: Dockerfile.prod
    env_file:
      - .env.staging
    ports:
      - "4200:4000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production