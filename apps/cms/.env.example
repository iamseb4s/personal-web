# ============================================================
# === CMS (Strapi) Environment Variables (.env.example) ===
# ============================================================
#
# Copy this file to .env.development, .env.staging, or .env.production
# and fill in the values for the corresponding environment.
#
# Do NOT commit any .env.* files with secrets to version control.
#

# --- Database ---
# Configuration for the SQLite database.
DATABASE_CLIENT=sqlite
DATABASE_FILENAME=./database/development.sqlite # Change to production.sqlite for staging/prod

# --- Server ---
# Network configuration for the Strapi server.
HOST=0.0.0.0
PORT=1337

# --- Public URL ---
# The public-facing URL of this Strapi instance.
# Used for generating correct links for media files, etc.
# Example for staging: https://stagingcms.yourdomain.com
URL=

# --- Security (MANDATORY for Staging/Production) ---
# These secrets are critical for running Strapi in a production environment.
# Generate each value using the command: openssl rand -base64 32

# Used for signing and encrypting session cookies and other sensitive data.
APP_KEYS=

# Salts and secrets for API tokens and JWTs.
API_TOKEN_SALT=
ADMIN_JWT_SECRET=
TRANSFER_TOKEN_SALT=
JWT_SECRET=
