# ===============================================================
# === Web App Environment Variables (.env.example) ===
# ===============================================================
#
# Copy this file to .env.development, .env.staging, or .env.production
# and fill in the values for the corresponding environment.
#
# Do NOT commit any .env.* files with secrets to version control.
#

# --- Strapi API URLs ---

# The internal URL for server-side rendering (SSR) and static site generation (SSG)
# to communicate with the Strapi container directly.
# Example for development: http://dev-cms:1337
# Example for staging: http://staging-cms:1337
STRAPI_INTERNAL_URL=

# The public-facing URL of the Strapi API.
# This is used by the client-side (browser) to fetch data, e.g., for images.
# Example for development: https://devcms.yourdomain.com
# Example for staging: https://stagingcms.yourdomain.com
NEXT_PUBLIC_STRAPI_API_URL=

# The public URL of the website, used for generating metadata (e.g., hreflang tags).
# Example for development: https://devweb.iamsebas.dev
NEXT_PUBLIC_SITE_URL=

# --- Authentication ---

# A Strapi API Token with "Full access" permissions.
# This is required for the Next.js app to fetch data from Strapi.
STRAPI_API_TOKEN=

# ==================================
# DOCKER CONTAINER NAMES (Optional)
# ==================================
# This variable defines the name of the Web Docker container.
# For local development, it can be left blank to use the default name defined in docker-compose.
WEB_CONTAINER_NAME= # Example: staging-web or prod-web
